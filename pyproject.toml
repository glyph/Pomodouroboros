[build-system]
requires = [
    "setuptools>=42",
    "py2app",
    "wheel",
    "quickmacapp",
]
setup-requires = [
    "py2app",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79

[tool.isort]
profile = "black"
line-length = 79
